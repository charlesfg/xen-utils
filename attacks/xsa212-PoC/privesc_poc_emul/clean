obj=module
for i in $( ls ${obj}[0-9].c);
do
    V=$(basename $i .c | sed "s/${obj}_//" | cat)
    bash -x compile.sh $V
    make clean
    rm ${obj}${V}.c
    rm attack${i}.c
done 
rm attack.c
rm *-safe
rm Makefile


